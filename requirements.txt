# Heat Exchanger Design Engine — Python >=3.11
# Install: pip install -r requirements.txt

# --- Core ---
pydantic>=2.0,<3.0
pydantic-settings>=2.1.0
anthropic>=0.40
motor>=3.3
pymongo>=4.6
python-dotenv>=1.0
fastapi>=0.115.0
uvicorn[standard]>=0.27.0
redis[hiredis]>=5.0.0
sse-starlette>=2.0.0
httpx>=0.27.0

# --- Thermo (optional — needed for fluid-property lookups) ---
iapws>=1.5
CoolProp>=6.4
thermo>=0.3

# --- Dev / Testing ---
pytest>=8.0
pytest-asyncio>=0.23
